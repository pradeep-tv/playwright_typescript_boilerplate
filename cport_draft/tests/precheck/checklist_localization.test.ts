import { test, expect } from "@playwright/test";

test.describe("Checklist name alert message", { tag: `@fieldValidation` }, () => {
    test("Verify alert message when checklist name is empty", async () => {
        console.log("Running tests for checklist name field");
    });
});
