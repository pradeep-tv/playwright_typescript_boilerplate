import { test, expect } from "@playwright/test";

test.describe("Select the Vehicle From menu", { tag: "@SMOKE" }, () => {
    
    test("COST0003 - Select the Vehicle From menu", async ({ page }) => {
        console.log("Vehicle From menu selected");
    });

});
