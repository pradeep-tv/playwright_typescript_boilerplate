import { test } from "@playwright/test";

test.describe("Localization tests for add driver modal", { tag: ["@DRI", "@Localization"] }, () => {
    test("Verify UI elements in add driver modal for locale: ", () => {
        console.log("Running tests for locale: ");
    });
});